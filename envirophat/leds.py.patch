--- leds.py	2024-11-23 12:45:30.892196961 -0800
+++ leds.py.new.py	2024-11-23 13:05:48.000000000 -0800
@@ -1,15 +1,13 @@
 from sys import exit
+from os import popen
 
 try:
-    import RPi.GPIO as GPIO
+    import gpiozero as GPIO
 except ImportError:
-    exit("This library requires the RPi.GPIO module\nInstall with: sudo pip install RPi.GPIO")
+    exit("This library requires the gpiozero module\nInstall with: sudo pip install gpiozero")
 
-
-GPIO.setwarnings(False)
-GPIO.setmode(GPIO.BCM)
-GPIO.setup(4, GPIO.OUT)
-GPIO.output(4, 0)
+led = GPIO.LED(4)
+led.off()
 
 class leds:
     def __init__(self, status=0):
@@ -18,13 +16,13 @@
     def on(self):
         """Turn LEDs on."""
         self.status = 1
-        GPIO.output(4, 1)
+        led.on()
         return True
 
     def off(self):
         """Turn LEDs off."""
         self.status = 0
-        GPIO.output(4, 0)
+        led.off()
 
     def is_on(self):
         """Return True if LED is on."""
